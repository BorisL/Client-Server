Pour compiler :
cd Debug
make all

Pour executer :
cd Debug
./Client host port [-n nickname] [-v]
@arg host : host où se connecter 
@arg port : port où se connecter
@arg nickname : nickname à utiliser sur le serveur

options :
-v : activer le debug dans le terminal
-n : utiliser un nickname 
